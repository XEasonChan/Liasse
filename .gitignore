# Local secrets / machine state
.env
.DS_Store
.claude/
CLAUDE.md

# Python environments and caches
venv/
.venv/
.venv-mlx/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/

# Generated runtime output
outputs/
models/
build/
dist/
*.db
*.sqlite
*.sqlite3
*.log

# Local evaluation media. Do not commit interview recordings.
test_audio/
*.mp3
*.wav
*.m4a
*.flac
*.aac
*.ogg
*.wma
*.mp4

# Historical local vendor snapshot; PyPI packages are used instead.
vendor/

# Local research reference repos (open-source projects we read for inspiration,
# never bundled into Qwensper). Can be large (100MB+). Cloned ad-hoc per machine.
reference/

# Local agent planning notes.
docs/superpowers/

# Benchmark samples + GT + intermediate outputs (big, gitignored)
scripts/benchmark/samples/*.m4a
scripts/benchmark/samples/*.wav
scripts/benchmark/results/_pipeline_outputs/
